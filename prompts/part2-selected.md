# Selected Part 2 prompts

Concise paraphrases of important user instructions, not verbatim transcripts. Work remains on part2-sqlite-crud. Preserve instructor CSVs and Part 1 search; report each stage and stop for review without commit/push.

1. **Inspection/design:** Inspect the existing project and four CSVs before editing. Confirm preserved Part 1 commit 2c513110aa26d171b44f8de1682abf0208c9f7f8, columns/counts, IDs, and relationships. Propose SQLite and MVC using instructor columns exactly.
2. **SQLite foundation:** Use built-in sqlite3 and hotels/trips/users/bookings with foreign keys. Seed once at initialization; preserve changes and never reload starter rows on later starts. Ignore generated database files. Test counts 8/12/6/6, references, repeat initialization, and persistence.
3. **CRUD/MVC:** Add models and database controllers for users, unique-ID creation, joined history, cancellation retaining records, and generated test-booking deletion. Use parameterized SQL and transactions; test errors and persistence.
4. **FastAPI:** Add thin GET /api/users, POST /api/bookings (201), GET /api/bookings?user_id=..., PATCH /api/bookings/{booking_id} accepting cancelled only, and DELETE /api/bookings/{booking_id} (204). Return 404 for missing records, 422 for invalid requests, and a clear client error for protected deletion.
5. **Vue CRUD:** Keep hotel search. Add demo-user selection, Book stay, history, cancellation, deletion confirmation, and clear success/error/loading states using small components, composables, and API modules. Preserve backend behavior and CSVs.
6. **UI Research/redesign:** Inspect Expedia, Priceline, and Booking.com in the embedded browser first. Adopt relevant search/results/action/history patterns. Implement compact branding, labelled user selection, Find a stay, clearer results, separate history, status badges, visible smaller IDs, and distinct Cancel/Delete with Keep booking and Confirm delete. Exclude unrelated travel features, authentication, payment, surge pricing, maps, reviews, fake photos, and invented prices.
7. **Verification:** Run frontend tests, read-only lint, production build, backend tests, and Git whitespace checks. Inspect search/CRUD wiring and report blockers. Start/restart only project services without resetting/reseeding SQLite. Record user-reported manual CRUD and persistence without claiming keyboard interaction was manually verified.
